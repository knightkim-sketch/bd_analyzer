diff --git a/YUViewLib/YUViewLib.pro b/YUViewLib/YUViewLib.pro
--- a/YUViewLib/YUViewLib.pro
+++ b/YUViewLib/YUViewLib.pro
@@ -22,6 +22,7 @@
 #   src/assist/       Qt-free assistant panel context model, unit tested on its own
 #   src/container/    Qt-free MP4 box + sample table parser, unit tested on its own
 #   src/bitstream/    Qt-free AV1 bitstream reads, unit tested on its own
+#   src/yt/           Qt-free YouTube link list parsing, unit tested on its own
 #   src/integration/  the only part of our tree allowed to know about upstream types
 #
 # $$PWD is .../third_party/yuview/upstream/YUViewLib, so four levels up is the repository root.
@@ -32,12 +33,14 @@
 SOURCES += $$files($$BDA_ROOT/src/assist/*.cpp, true)
 SOURCES += $$files($$BDA_ROOT/src/container/*.cpp, true)
 SOURCES += $$files($$BDA_ROOT/src/bitstream/*.cpp, true)
+SOURCES += $$files($$BDA_ROOT/src/yt/*.cpp, true)
 SOURCES += $$files($$BDA_ROOT/src/integration/*.cpp, true)
 HEADERS += $$files($$BDA_ROOT/src/me/*.h, true)
 HEADERS += $$files($$BDA_ROOT/src/bdrate/*.h, true)
 HEADERS += $$files($$BDA_ROOT/src/assist/*.h, true)
 HEADERS += $$files($$BDA_ROOT/src/container/*.h, true)
 HEADERS += $$files($$BDA_ROOT/src/bitstream/*.h, true)
+HEADERS += $$files($$BDA_ROOT/src/yt/*.h, true)
 HEADERS += $$files($$BDA_ROOT/src/integration/*.h, true)
 
 RESOURCES += \
diff --git a/YUViewLib/ui/mainwindow.ui b/YUViewLib/ui/mainwindow.ui
--- a/YUViewLib/ui/mainwindow.ui
+++ b/YUViewLib/ui/mainwindow.ui
@@ -416,6 +416,24 @@
    </attribute>
    <widget class="AssistPanelWidget" name="assistPanelWidget"/>
   </widget>
+  <widget class="QDockWidget" name="ytTranscodeDock">
+   <property name="visible">
+    <bool>false</bool>
+   </property>
+   <property name="minimumSize">
+    <size>
+     <width>340</width>
+     <height>360</height>
+    </size>
+   </property>
+   <property name="windowTitle">
+    <string>YouTube Transcode</string>
+   </property>
+   <attribute name="dockWidgetArea">
+    <number>2</number>
+   </attribute>
+   <widget class="YtTranscodeWidget" name="ytTranscodeWidget"/>
+  </widget>
   <action name="actionOpen">
    <property name="text">
     <string>Open...</string>
@@ -449,6 +467,12 @@
    <container>1</container>
   </customwidget>
   <customwidget>
+   <class>YtTranscodeWidget</class>
+   <extends>QWidget</extends>
+   <header>integration/YtTranscodeWidget.h</header>
+   <container>1</container>
+  </customwidget>
+  <customwidget>
    <class>PlaylistTreeWidget</class>
    <extends>QTreeWidget</extends>
    <header>ui/widgets/PlaylistTreeWidget.h</header>
diff --git a/YUViewLib/src/ui/Mainwindow.cpp b/YUViewLib/src/ui/Mainwindow.cpp
--- a/YUViewLib/src/ui/Mainwindow.cpp
+++ b/YUViewLib/src/ui/Mainwindow.cpp
@@ -809,6 +809,11 @@
    * panel suggests is taken by a dock or a zoom action.
    */
   addDockViewAction(ui.assistDock, "Show AI Assistan&t", Qt::CTRL | Qt::Key_K);
+  /* Ctrl+Y for YouTube. Not in the Dock Panels group above by accident - it is a source of
+   * material rather than a view of the item on screen, but it is still a dock and belongs with
+   * the other docks rather than in a menu of its own.
+   */
+  addDockViewAction(ui.ytTranscodeDock, "Show &YouTube Transcode", Qt::CTRL | Qt::Key_Y);
   viewMenu->addSeparator();
   addDockViewAction(ui.playbackControllerDock, "Show Playback &Controls", Qt::CTRL | Qt::Key_D);
 
